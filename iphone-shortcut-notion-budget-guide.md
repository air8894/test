# 아이폰 단축어 + 노션 연동 가계부 (매일 수입/지출 입력 → 월별 현황 파악)

매일 수입·지출을 아이폰 단축어(Shortcuts)로 빠르게 입력하면, 노션(Notion) 데이터베이스에 자동으로 쌓이고, 노션의 필터·그룹·합계 기능으로 월별 현황을 바로 확인할 수 있는 구조입니다.

전체 흐름:

```
[아이폰 단축어 실행]
   → 날짜 / 구분(수입·지출) / 카테고리 / 금액 / 메모 입력
   → 노션 API로 새 페이지(행) 생성
   → 노션 데이터베이스에 자동 기록
   → 노션에서 "이번 달" 보기로 합계·카테고리별 통계 확인
```

---

## 1. 노션 데이터베이스 만들기

노션에서 새 페이지를 만들고 **테이블(데이터베이스)** 를 추가합니다. 예: `가계부`

| 속성명 | 타입 | 설명 |
|---|---|---|
| 이름(제목) | 제목(Title) | 예: "점심식사", "월급" 등 항목명 |
| 날짜 | 날짜(Date) | 입력한 날짜 |
| 구분 | 선택(Select) | `수입` / `지출` 두 옵션 |
| 카테고리 | 선택(Select) | 식비, 교통, 월급, 용돈, 고정지출 등 |
| 금액 | 숫자(Number) | 원화, 통화 형식 권장 |
| 메모 | 텍스트(Text) | 선택 입력 |

> 팁: `금액` 속성에서 `수입`이면 양수, `지출`이면 음수로 저장하도록 단축어에서 계산해 넣으면, 노션에서 합계(Sum)만 걸어도 "순수입"이 바로 나옵니다.

### 월별 요약 보기 만들기
데이터베이스 상단에서 **+ 보기 추가** → **테이블** 또는 **캘린더**를 선택하고,
- **필터**: 날짜가 "이번 달" 인 것만
- **그룹화**: `구분` 기준으로 그룹화(수입/지출 나눠보기)
- 그룹 하단에 **합계(Sum)** 계산 표시 (속성 열 하단을 클릭하면 Sum/Average 등 선택 가능)

이렇게 하면 "이번 달 수입 합계 / 지출 합계"가 보기 하나로 자동 집계됩니다. 매달 새 보기를 만들 필요 없이, 필터를 "지난 30일" 또는 "이번 달"로 고정해두면 계속 재사용됩니다.

(선택) 더 정교한 월별 리포트가 필요하면 `월별 요약` 이라는 별도 데이터베이스를 만들고, `가계부` 데이터베이스와 **Relation**으로 연결한 뒤 **Rollup** 속성으로 월별 합계를 자동 계산할 수도 있습니다.

---

## 2. 노션 API 연동 준비 (Integration 발급)

아이폰 단축어가 노션에 데이터를 쓰려면 **Notion API 토큰**과 **데이터베이스 ID**가 필요합니다.

1. https://www.notion.so/my-integrations 접속 → **+ New integration** 클릭
2. 이름(예: `가계부 단축어`) 입력, 연결할 워크스페이스 선택 → **제출**
3. 생성된 **Internal Integration Secret**(토큰, `ntn_...` 또는 `secret_...` 형태)을 복사해둡니다.
4. 노션에서 `가계부` 데이터베이스 페이지로 이동 → 우측 상단 **···** 메뉴 → **연결 추가(Add connection)** → 방금 만든 Integration을 선택해 **권한 부여**
   - 이 단계를 빠뜨리면 API 요청이 401(권한 없음) 에러가 납니다.
5. 데이터베이스 ID 확인: 데이터베이스 페이지를 브라우저(또는 "링크 복사")로 열면 URL이 아래와 같은 형태입니다.
   ```
   https://www.notion.so/워크스페이스명/1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d?v=...
                                       └────────── 이 32자리가 데이터베이스 ID ──────────┘
   ```

---

## 3. 아이폰 단축어(Shortcuts) 만들기

**단축어 앱** → **+ (새 단축어 만들기)** → 이름을 `가계부 입력` 등으로 설정 후 아래 순서로 액션을 추가합니다.

### 3-1. 입력값 받기
1. **메뉴에서 선택(Choose from Menu)** 액션 추가 → 메뉴 항목 `수입`, `지출` 두 개로 설정 → 각 항목에 변수 저장(예: `구분`)
2. **텍스트 입력받기(Ask for Input)** – 유형: 숫자 → 프롬프트 "금액이 얼마인가요?" → 변수명 `금액`
3. **메뉴에서 선택** 또는 **목록에서 선택(Choose from List)** – 카테고리 목록(식비, 교통, 월급 등)을 미리 텍스트 목록으로 만들어두고 선택 → 변수 `카테고리`
4. **텍스트 입력받기** – "메모(선택)" → 변수 `메모` (건너뛰기 허용)
5. **현재 날짜 가져오기(Current Date)** 액션 추가 → **날짜 형식 지정(Format Date)** 으로 `yyyy-MM-dd` 형식으로 변환 → 변수 `날짜`

### 3-2. 금액에 부호 적용 (선택)
**구분** 값이 `지출`이면 금액을 음수로 바꾸고 싶다면:
- **만약(If)** 액션: `구분` = `지출` → **계산하기(Calculate)**: `금액 × -1` → 결과를 `금액`에 저장
- **그 외(Otherwise)**: 그대로 사용
- **만약 끝(End If)**

### 3-3. 노션 API 요청 만들기
1. **사전(Dictionary)** 액션을 추가해 아래처럼 JSON 구조를 구성합니다 (단축어 앱에서 "사전" 안에 "사전"을 중첩해서 만들 수 있습니다).

```json
{
  "parent": { "database_id": "여기에_데이터베이스_ID" },
  "properties": {
    "이름": { "title": [ { "text": { "content": "카테고리 변수" } } ] },
    "날짜": { "date": { "start": "날짜 변수" } },
    "구분": { "select": { "name": "구분 변수" } },
    "카테고리": { "select": { "name": "카테고리 변수" } },
    "금액": { "number": 금액 변수 },
    "메모": { "rich_text": [ { "text": { "content": "메모 변수" } } ] }
  }
}
```
   실제 단축어에서는 위 JSON을 직접 타이핑하지 않고, "사전" 액션의 각 키(Key)-값(Value) 입력란에 앞서 만든 변수를 끼워 넣는 방식으로 만듭니다.

2. **URL 콘텐츠 가져오기(Get Contents of URL)** 액션 추가:
   - **URL**: `https://api.notion.com/v1/pages`
   - **방법(Method)**: `POST`
   - **헤더(Headers)**:
     - `Authorization` → `Bearer 여기에_Integration_토큰`
     - `Notion-Version` → `2022-06-28`
     - `Content-Type` → `application/json`
   - **요청 본문(Request Body)**: `JSON` 선택 → 위에서 만든 **사전** 변수를 그대로 삽입

3. (선택) **만약(If)** 응답에 오류가 있으면 **알림 표시(Show Notification)** 로 "저장 실패, 다시 시도해주세요" 안내, 성공 시 "가계부에 저장됨 ✅" 알림 표시.

### 3-4. 빠르게 실행하는 법
- 단축어 설정에서 **홈 화면에 추가**하면 아이콘 탭 한 번으로 실행됩니다.
- **"Siri에 추가"** 로 "시리야, 가계부 입력해줘" 같은 음성 명령도 가능합니다.
- **자동화(Automation) 탭** → **개인 자동화 만들기** → **시간(매일 저녁 9시)** 트리거로 설정하면, 매일 저녁 알림이 오고 탭 한 번으로 오늘 지출을 입력하도록 유도할 수 있습니다. (알림 후 확인 없이 바로 실행되게 하려면 "실행 전에 확인" 옵션을 꺼주세요.)

---

## 4. 월별 현황 확인하기

노션 앱/웹에서 `가계부` 데이터베이스를 열고:

1. 상단 보기 탭에서 **이번 달 요약** 보기(1단계에서 만든 필터+그룹 보기)를 선택
2. `구분`별로 그룹화되어 있으므로 `수입` 그룹과 `지출` 그룹 각각의 **합계(Sum)** 를 바로 확인
3. `카테고리`로 2차 그룹화(그룹 추가)하면 "이번 달 식비 총액", "이번 달 교통비 총액" 등도 한눈에 확인 가능
4. 순자산 흐름을 보려면 `금액`을 지출은 음수로 저장해뒀을 경우, 전체 합계가 바로 "이번 달 순수입"이 됩니다.
5. 매달 리포트를 쌓아두고 싶다면, 캘린더 보기 + 타임라인 보기를 조합해서 월별 추이를 시각적으로도 확인할 수 있습니다.

---

## 5. 확장 아이디어

- **주간/월간 자동 리포트**: 노션 자동화(Notion Automations)로 매월 1일 "지난달 요약" 알림을 워크스페이스에 보내도록 설정
- **카테고리 예산 관리**: `카테고리별 예산` 데이터베이스를 추가하고 Rollup으로 "예산 대비 사용률" 계산
- **위젯**: 아이폰 홈 화면에 단축어 위젯을 추가해 최근 지출 요약을 바로 보이게 구성
- **가족 공유**: 노션 데이터베이스를 가족과 공유하고, 각자 자신의 아이폰에 동일한 단축어(Integration 토큰만 개별 발급)를 설치해 공동 가계부로 활용

---

## 참고: 문제 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 401 Unauthorized | Integration이 데이터베이스에 연결되지 않음 | 데이터베이스 `···` → 연결 추가에서 Integration 권한 부여 |
| 400 Bad Request | JSON 속성명이 노션 속성명과 불일치 | 노션 데이터베이스의 속성명(대소문자 포함)과 정확히 일치시키기 |
| Select 값 오류 | `구분`/`카테고리`에 존재하지 않는 옵션명 전달 | 노션에서 미리 만들어둔 옵션명과 단축어의 텍스트가 정확히 같아야 함 (자동 생성되진 않음, 최초 1회는 노션에서 옵션을 만들어두는 것을 권장) |
